{
  "id": "baseten-docs",
  "name": "Baseten Docs",
  "url": "https://docs.baseten.co/mcp",
  "homepage": "https://baseten.co",
  "platform": "mintlify-docs",
  "transport": "streamable-http",
  "status": "ok",
  "error": null,
  "protocolVersion": null,
  "serverInfo": null,
  "provenance": {
    "at": "2026-09-16T17:04:10.337Z",
    "declaredUrl": "https://docs.baseten.co/mcp",
    "observedUrl": "https://docs.baseten.co/mcp",
    "urlMatchesDeclared": true,
    "redirected": false,
    "redirects": [],
    "via": "direct-post",
    "envelope": "sse",
    "methodChangingRedirect": false,
    "refused": null
  },
  "tools": [
    {
      "name": "query_docs_filesystem_baseten",
      "title": null,
      "description": "Run a read-only shell-like query against a virtualized, in-memory filesystem rooted at `/` that contains ONLY the Baseten documentation pages and OpenAPI specs. This is NOT a shell on any real machine — nothing runs on the user's computer, the server host, or any network. The filesystem is a sandbox backed by documentation chunks.\n\nThis is how you read documentation pages: there is no separate \"get page\" tool. To read a page, pass its `.mdx` path to `head` or `cat` — a page at the URL path `/some/page` lives at `/some/page.mdx`. To search the docs with exact keyword or regex matches, use `rg`. To understand the docs structure, use `tree` or `ls`.\n\n**Paths are specific to this site — never guess them.** Discover real paths with `tree / -L 2`, `ls /`, or the search tool before reading. If a path does not exist, that only means the guess was wrong; it does NOT mean the topic is undocumented — use `rg -il \"keyword\" /` to find where it is covered.\n\n**Workflow:** Start with the search tool for broad or conceptual queries like \"how to authenticate\" or \"rate limiting\". Use this tool when you need exact keyword/regex matching, structural exploration, or to read the full content of a specific page by path.\n\nSupported commands: rg (ripgrep), grep, find, tree, ls, cat, head, tail, stat, wc, sort, uniq, cut, sed, awk, jq, plus basic text utilities. No writes, no network, no process control. Run `--help` on any command for usage.\n\nEach call is STATELESS: the working directory always resets to `/` and no shell variables, aliases, or history carry over between calls. If you need to operate in a subdirectory, chain commands in one call with `&&` or pass absolute paths (e.g., `cd /some-directory && ls` or `ls /some-directory`). Do NOT assume that `cd` in one call affects the next call.\n\nExamples (replace the placeholder paths with real ones from `tree` or search):\n- `tree / -L 2` — see the top-level directory layout\n- `rg -il \"rate limit\" /` — find all files mentioning \"rate limit\"\n- `rg -C 3 \"apiKey\" /some-directory/` — show matches with 3 lines of context around each hit\n- `head -80 /some/page.mdx` — read the top 80 lines of a specific page\n- `head -80 /page-one.mdx /page-two.mdx /section/page-three.mdx` — read multiple pages in one call\n- `cat /some/page.mdx` — read a full page when you need everything\n- `cat /openapi/reference/inference-api/messages-openapi-spec.json | jq '.paths | keys'` — list OpenAPI endpoints\n\nOpenAPI specs for this site are mounted at: `'/openapi/https://api.baseten.co/v1/spec'`, `/openapi/reference/inference-api/messages-openapi-spec.json`, `/openapi/reference/inference-api/llm-openapi-spec.json`, `/openapi/reference/inference-api/inference-api-spec.json`, `/openapi/spec`. Use them to answer questions about endpoints, request/response schemas, parameters, and authentication.\n\nOutput is truncated to 30KB per call. Prefer targeted `rg -C` or `head -N` over broad `cat` on large files. To read only the relevant sections of a large file, use `rg -C 3 \"pattern\" /path/file.mdx`. Batch multiple file reads into a single `head` or `cat` call whenever possible.\n\nWhen referencing pages in your response to the user, convert filesystem paths to URL paths by removing the `.mdx` extension. For example, `/some/page.mdx` becomes `/some/page`.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "description": "A shell command to run against the virtualized documentation filesystem (e.g., `rg -il \"keyword\" /`, `tree / -L 2`, `head -80 /path/file.mdx`)."
          }
        },
        "required": [
          "command"
        ],
        "additionalProperties": false
      },
      "outputSchema": null,
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "schemaFingerprint": "ab203162be885ac7"
    },
    {
      "name": "search_baseten",
      "title": "Search documentation",
      "description": "Search across the Baseten knowledge base to find relevant information, code examples, API references, and guides. Use this tool when you need to answer questions about Baseten, find specific documentation, understand how features work, or locate implementation details. The search returns contextual content with titles and direct links to the documentation pages. If you need the full content of a specific page, use the query_docs_filesystem tool to `head` or `cat` the page path (append `.mdx` to the path returned from search — e.g. a result at `/some/page` is read with `head -200 /some/page.mdx`).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query"
          }
        },
        "required": [
          "query"
        ],
        "additionalProperties": false
      },
      "outputSchema": null,
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "schemaFingerprint": "cb69ec950d34e626"
    },
    {
      "name": "submit_feedback",
      "title": "Submit documentation feedback",
      "description": "Report a problem with this documentation site so the docs team can fix it. Use when a documentation page is incorrect, outdated, confusing, incomplete, or has a broken example. This is for feedback about the documentation content itself — not for product support requests or feedback about this tool or assistant.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "The URL path of the documentation page the feedback is about (the page you were reading, without the `.mdx` extension)."
          },
          "feedback": {
            "type": "string",
            "minLength": 1,
            "description": "A clear description of the documentation issue or suggestion — what is incorrect, outdated, missing, or confusing."
          }
        },
        "required": [
          "path",
          "feedback"
        ],
        "additionalProperties": false
      },
      "outputSchema": null,
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      },
      "schemaFingerprint": "20eeaaefbd1b2f1d"
    }
  ],
  "toolCount": 3,
  "fingerprint": "04093684364656de",
  "lastGood": {
    "at": "2026-09-16T17:04:10.337Z",
    "tools": [
      {
        "name": "query_docs_filesystem_baseten",
        "title": null,
        "description": "Run a read-only shell-like query against a virtualized, in-memory filesystem rooted at `/` that contains ONLY the Baseten documentation pages and OpenAPI specs. This is NOT a shell on any real machine — nothing runs on the user's computer, the server host, or any network. The filesystem is a sandbox backed by documentation chunks.\n\nThis is how you read documentation pages: there is no separate \"get page\" tool. To read a page, pass its `.mdx` path to `head` or `cat` — a page at the URL path `/some/page` lives at `/some/page.mdx`. To search the docs with exact keyword or regex matches, use `rg`. To understand the docs structure, use `tree` or `ls`.\n\n**Paths are specific to this site — never guess them.** Discover real paths with `tree / -L 2`, `ls /`, or the search tool before reading. If a path does not exist, that only means the guess was wrong; it does NOT mean the topic is undocumented — use `rg -il \"keyword\" /` to find where it is covered.\n\n**Workflow:** Start with the search tool for broad or conceptual queries like \"how to authenticate\" or \"rate limiting\". Use this tool when you need exact keyword/regex matching, structural exploration, or to read the full content of a specific page by path.\n\nSupported commands: rg (ripgrep), grep, find, tree, ls, cat, head, tail, stat, wc, sort, uniq, cut, sed, awk, jq, plus basic text utilities. No writes, no network, no process control. Run `--help` on any command for usage.\n\nEach call is STATELESS: the working directory always resets to `/` and no shell variables, aliases, or history carry over between calls. If you need to operate in a subdirectory, chain commands in one call with `&&` or pass absolute paths (e.g., `cd /some-directory && ls` or `ls /some-directory`). Do NOT assume that `cd` in one call affects the next call.\n\nExamples (replace the placeholder paths with real ones from `tree` or search):\n- `tree / -L 2` — see the top-level directory layout\n- `rg -il \"rate limit\" /` — find all files mentioning \"rate limit\"\n- `rg -C 3 \"apiKey\" /some-directory/` — show matches with 3 lines of context around each hit\n- `head -80 /some/page.mdx` — read the top 80 lines of a specific page\n- `head -80 /page-one.mdx /page-two.mdx /section/page-three.mdx` — read multiple pages in one call\n- `cat /some/page.mdx` — read a full page when you need everything\n- `cat /openapi/reference/inference-api/messages-openapi-spec.json | jq '.paths | keys'` — list OpenAPI endpoints\n\nOpenAPI specs for this site are mounted at: `'/openapi/https://api.baseten.co/v1/spec'`, `/openapi/reference/inference-api/messages-openapi-spec.json`, `/openapi/reference/inference-api/llm-openapi-spec.json`, `/openapi/reference/inference-api/inference-api-spec.json`, `/openapi/spec`. Use them to answer questions about endpoints, request/response schemas, parameters, and authentication.\n\nOutput is truncated to 30KB per call. Prefer targeted `rg -C` or `head -N` over broad `cat` on large files. To read only the relevant sections of a large file, use `rg -C 3 \"pattern\" /path/file.mdx`. Batch multiple file reads into a single `head` or `cat` call whenever possible.\n\nWhen referencing pages in your response to the user, convert filesystem paths to URL paths by removing the `.mdx` extension. For example, `/some/page.mdx` becomes `/some/page`.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "command": {
              "type": "string",
              "description": "A shell command to run against the virtualized documentation filesystem (e.g., `rg -il \"keyword\" /`, `tree / -L 2`, `head -80 /path/file.mdx`)."
            }
          },
          "required": [
            "command"
          ],
          "additionalProperties": false
        },
        "outputSchema": null,
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "schemaFingerprint": "ab203162be885ac7"
      },
      {
        "name": "search_baseten",
        "title": "Search documentation",
        "description": "Search across the Baseten knowledge base to find relevant information, code examples, API references, and guides. Use this tool when you need to answer questions about Baseten, find specific documentation, understand how features work, or locate implementation details. The search returns contextual content with titles and direct links to the documentation pages. If you need the full content of a specific page, use the query_docs_filesystem tool to `head` or `cat` the page path (append `.mdx` to the path returned from search — e.g. a result at `/some/page` is read with `head -200 /some/page.mdx`).",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Search query"
            }
          },
          "required": [
            "query"
          ],
          "additionalProperties": false
        },
        "outputSchema": null,
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "schemaFingerprint": "cb69ec950d34e626"
      },
      {
        "name": "submit_feedback",
        "title": "Submit documentation feedback",
        "description": "Report a problem with this documentation site so the docs team can fix it. Use when a documentation page is incorrect, outdated, confusing, incomplete, or has a broken example. This is for feedback about the documentation content itself — not for product support requests or feedback about this tool or assistant.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "minLength": 1,
              "description": "The URL path of the documentation page the feedback is about (the page you were reading, without the `.mdx` extension)."
            },
            "feedback": {
              "type": "string",
              "minLength": 1,
              "description": "A clear description of the documentation issue or suggestion — what is incorrect, outdated, missing, or confusing."
            }
          },
          "required": [
            "path",
            "feedback"
          ],
          "additionalProperties": false
        },
        "outputSchema": null,
        "annotations": {
          "readOnlyHint": false,
          "destructiveHint": false,
          "idempotentHint": false,
          "openWorldHint": true
        },
        "schemaFingerprint": "20eeaaefbd1b2f1d"
      }
    ],
    "fingerprint": "04093684364656de",
    "protocolVersion": null
  },
  "firstSeenAt": "2026-09-09T10:55:17.881Z",
  "lastCheckedAt": "2026-09-16T17:04:10.337Z",
  "lastOkAt": "2026-09-16T17:04:10.337Z",
  "lastChangedAt": "2026-09-16T17:04:10.337Z",
  "changeCount": 3,
  "card": {
    "id": "baseten-docs",
    "cardUrl": "https://docs.baseten.co/.well-known/mcp/server-card.json",
    "at": "2026-09-16T17:04:10.337Z",
    "cardPath": "/.well-known/mcp/server-card.json",
    "status": "ok",
    "error": null,
    "card": {
      "schema": null,
      "name": "Baseten Docs MCP",
      "version": "1.0.0",
      "endpoints": [
        "https://baseten-preview.main-kill-isr.mintlify.me/mcp"
      ],
      "remotes": null,
      "tools": [
        {
          "name": "query_docs_filesystem_baseten",
          "description": "Run a read-only shell-like query against a virtualized, in-memory filesystem rooted at `/` that contains ONLY the Baseten documentation pages and OpenAPI specs. This is NOT a shell on any real machine — nothing runs on the user's computer, the server host, or any network. The filesystem is a sandbox backed by documentation chunks.\n\nThis is how you read documentation pages: there is no separate \"get page\" tool. To read a page, pass its `.mdx` path to `head` or `cat` — a page at the URL path `/some/page` lives at `/some/page.mdx`. To search the docs with exact keyword or regex matches, use `rg`. To understand the docs structure, use `tree` or `ls`.\n\n**Paths are specific to this site — never guess them.** Discover real paths with `tree / -L 2`, `ls /`, or the search tool before reading. If a path does not exist, that only means the guess was wrong; it does NOT mean the topic is undocumented — use `rg -il \"keyword\" /` to find where it is covered.\n\n**Workflow:** Start with the search tool for broad or conceptual queries like \"how to authenticate\" or \"rate limiting\". Use this tool when you need exact keyword/regex matching, structural exploration, or to read the full content of a specific page by path.\n\nSupported commands: rg (ripgrep), grep, find, tree, ls, cat, head, tail, stat, wc, sort, uniq, cut, sed, awk, jq, plus basic text utilities. No writes, no network, no process control. Run `--help` on any command for usage.\n\nEach call is STATELESS: the working directory always resets to `/` and no shell variables, aliases, or history carry over between calls. If you need to operate in a subdirectory, chain commands in one call with `&&` or pass absolute paths (e.g., `cd /some-directory && ls` or `ls /some-directory`). Do NOT assume that `cd` in one call affects the next call.\n\nExamples (replace the placeholder paths with real ones from `tree` or search):\n- `tree / -L 2` — see the top-level directory layout\n- `rg -il \"rate limit\" /` — find all files mentioning \"rate limit\"\n- `rg -C 3 \"apiKey\" /some-directory/` — show matches with 3 lines of context around each hit\n- `head -80 /some/page.mdx` — read the top 80 lines of a specific page\n- `head -80 /page-one.mdx /page-two.mdx /section/page-three.mdx` — read multiple pages in one call\n- `cat /some/page.mdx` — read a full page when you need everything\n- `cat /openapi/reference/inference-api/messages-openapi-spec.json | jq '.paths | keys'` — list OpenAPI endpoints\n\nOpenAPI specs for this site are mounted at: `'/openapi/https://api.baseten.co/v1/spec'`, `/openapi/reference/inference-api/messages-openapi-spec.json`, `/openapi/reference/inference-api/llm-openapi-spec.json`, `/openapi/reference/inference-api/inference-api-spec.json`, `/openapi/styles/proselint/meta.json`, `/openapi/styles/proselint/Very.yml`, `/openapi/styles/proselint/Uncomparables.yml`, `/openapi/styles/proselint/Typography.yml`, `/openapi/styles/proselint/Spelling.yml`, `/openapi/styles/proselint/Skunked.yml`, `/openapi/styles/proselint/RASSyndrome.yml`, `/openapi/styles/proselint/P-Value.yml`, `/openapi/styles/proselint/Oxymorons.yml`, `/openapi/styles/proselint/Nonwords.yml`, `/openapi/styles/proselint/Needless.yml`, `/openapi/styles/proselint/Malapropisms.yml`, `/openapi/styles/proselint/LGBTTerms.yml`, `/openapi/styles/proselint/LGBTOffensive.yml`, `/openapi/styles/proselint/Jargon.yml`, `/openapi/styles/proselint/Hyperbole.yml`, `/openapi/styles/proselint/Hedging.yml`, `/openapi/styles/proselint/GroupTerms.yml`, `/openapi/styles/proselint/GenderBias.yml`, `/openapi/styles/proselint/Diacritical.yml`, `/openapi/styles/proselint/DenizenLabels.yml`, `/openapi/styles/proselint/DateSpacing.yml`, `/openapi/styles/proselint/DateRedundancy.yml`, `/openapi/styles/proselint/DateMidnight.yml`, `/openapi/styles/proselint/DateCase.yml`, `/openapi/styles/proselint/Cursing.yml`, `/openapi/styles/proselint/Currency.yml`, `/openapi/styles/proselint/CorporateSpeak.yml`, `/openapi/styles/proselint/Cliches.yml`, `/openapi/styles/proselint/But.yml`, `/openapi/styles/proselint/Archaisms.yml`, `/openapi/styles/proselint/Apologizing.yml`, `/openapi/styles/proselint/Annotations.yml`, `/openapi/styles/proselint/AnimalLabels.yml`, `/openapi/styles/proselint/Airlinese.yml`, `/openapi/styles/Google/meta.json`, `/openapi/styles/Google/WordList.yml`, `/openapi/styles/Google/Will.yml`, `/openapi/styles/Google/We.yml`, `/openapi/styles/Google/Units.yml`, `/openapi/styles/Google/Spelling.yml`, `/openapi/styles/Google/Spacing.yml`, `/openapi/styles/Google/Slang.yml`, `/openapi/styles/Google/Semicolons.yml`, `/openapi/styles/Google/Ranges.yml`, `/openapi/styles/Google/Quotes.yml`, `/openapi/styles/Google/Periods.yml`, `/openapi/styles/Google/Passive.yml`, `/openapi/styles/Google/Parens.yml`, `/openapi/styles/Google/OxfordComma.yml`, `/openapi/styles/Google/Ordinal.yml`, `/openapi/styles/Google/OptionalPlurals.yml`, `/openapi/styles/Google/LyHyphens.yml`, `/openapi/styles/Google/Latin.yml`, `/openapi/styles/Google/Headings.yml`, `/openapi/styles/Google/HeadingPunctuation.yml`, `/openapi/styles/Google/GenderBias.yml`, `/openapi/styles/Google/Gender.yml`, `/openapi/styles/Google/FirstPerson.yml`, `/openapi/styles/Google/Exclamation.yml`, `/openapi/styles/Google/EmDash.yml`, `/openapi/styles/Google/Ellipses.yml`, `/openapi/styles/Google/DateFormat.yml`, `/openapi/styles/Google/Contractions.yml`, `/openapi/styles/Google/Colons.yml`, `/openapi/styles/Google/Acronyms.yml`, `/openapi/styles/Google/AMPM.yml`, `/openapi/package.json`, `/openapi/package-lock.json`, `/openapi/.vscode/settings.json`, `/openapi/spec`. Use them to answer questions about endpoints, request/response schemas, parameters, and authentication.\n\nOutput is truncated to 30KB per call. Prefer targeted `rg -C` or `head -N` over broad `cat` on large files. To read only the relevant sections of a large file, use `rg -C 3 \"pattern\" /path/file.mdx`. Batch multiple file reads into a single `head` or `cat` call whenever possible.\n\nWhen referencing pages in your response to the user, convert filesystem paths to URL paths by removing the `.mdx` extension. For example, `/some/page.mdx` becomes `/some/page`.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "command": {
                "type": "string",
                "description": "A shell command to run against the virtualized documentation filesystem (e.g., `rg -il \"keyword\" /`, `tree / -L 2`, `head -80 /path/file.mdx`)."
              }
            },
            "required": [
              "command"
            ]
          }
        },
        {
          "name": "search_baseten",
          "description": "Search across the Baseten knowledge base to find relevant information, code examples, API references, and guides. Use this tool when you need to answer questions about Baseten, find specific documentation, understand how features work, or locate implementation details. The search returns contextual content with titles and direct links to the documentation pages. If you need the full content of a specific page, use the query_docs_filesystem tool to `head` or `cat` the page path (append `.mdx` to the path returned from search — e.g. a result at `/some/page` is read with `head -200 /some/page.mdx`).",
          "inputSchema": {
            "type": "object",
            "properties": {
              "query": {
                "type": "string",
                "description": "A query to search the content with."
              }
            },
            "required": [
              "query"
            ]
          }
        }
      ],
      "toolCount": 2
    },
    "provenance": {
      "at": "2026-09-16T17:04:10.337Z",
      "declaredUrl": "https://docs.baseten.co/.well-known/mcp/server-card.json",
      "observedUrl": "https://docs.baseten.co/.well-known/mcp/server-card.json",
      "urlMatchesDeclared": true,
      "redirected": false,
      "redirects": [],
      "via": "well-known-get",
      "envelope": "json",
      "methodChangingRedirect": false,
      "refused": null
    }
  }
}
